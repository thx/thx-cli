import { describe, after, it } from 'mocha'
import { expect } from 'chai'
// import web from '../src/commands/system/web'

describe('web', () => {
  after((done) => {
    done()
  })
  it('start(TODO)', async (done) => {
    // await web()
    expect({}).to.be.a('object')
    done()
  })
})
